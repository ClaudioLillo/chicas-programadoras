/**
	Retorne un objeto organizando las personas por edad
	la llave sera la edad y el valor un array con las personas.
*/
function organizeByAge(list) {}

/**
	Retorne un objeto organizando las personas por hobby
	la llave sera el hobby y el valor un array de personas.
*/
function organizeByHobby(list) {}

/**
	Retorne un objeto con la suma de la edad de las personas
	que tienen la misma edad.
*/
function sumByAge(list) {}

export { organizeByAge, organizeByHobby, sumByAge };
